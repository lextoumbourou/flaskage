# -*- coding: utf-8 -*-
{{% if source_code.comments %}}
"""
    {{{ application.name }}}.tests.test_{{{ application.name }}}
    ~~~~~~~~~~~~

    Unit tests for the home blueprint.

    :copyright: (c) {{{ application.year }}} {{{ author.name }}}.
    :license: {{{ application.license }}}, see LICENSE for more details.
"""
{{% endif %}}
from . import BaseTestCase


class TestHome(BaseTestCase):

    def test_index(self):
        response = self.client.get("/")
        assert b'{{{ application.friendly_name }}}' in response.data

    def test_index_pyjade(self):
        response = self.client.get("/pyjade")
        assert b'{{{ application.friendly_name }}}' in response.data
